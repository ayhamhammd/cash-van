node_modules
dist
coverage
.env
.env.local
.env.*.local
*.log
.DS_Store
.idea
.vscode
# Runtime object-storage data dir at the repo root (STORAGE_LOCAL_ROOT=./storage).
# Anchored with a leading slash so it does NOT match the src/common/storage
# source module (that omission is what broke the build previously).
/storage/

# Windows deployment bundle produced by scripts/build-windows-bundle.sh.
# Contains a ~260MB docker image tarball — a build artifact, not source.
dist-windows/
